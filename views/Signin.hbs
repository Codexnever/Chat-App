<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Signin</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: rgb(255, 230, 8);
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }

        .container {
            background-color: #fff;
            padding: 30px 20px;
            border-radius: 10px;
            box-shadow: 0px 0px 20px rgba(0, 0, 0, 0.1);
            animation: fadeIn 1s ease-out;
        }

        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(-20px); }
            to { opacity: 1; transform: translateY(0); }
        }

        h1 {
            text-align: center;
            margin-bottom: 20px;
        }

        form {
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        input[type="text"],
        input[type="password"] {
            width: 300px;
            padding: 15px 40px 15px 25px; /* Adjusted padding */
            margin-bottom: 20px;
            border: none;
            border-bottom: 2px solid #ccc;
            outline: none;
            position: relative;
            transition: all 0.3s;
        }

        input[type="text"]::placeholder,
        input[type="password"]::placeholder {
            color: #ccc;
        }

        input[type="text"]:focus,
        input[type="password"]:focus {
            border-bottom: 2px solid #ffc107;
        }

        input[type="text"] + i,
        input[type="password"] + i {
            position: absolute;
            top: 50%;
            left: 10px; /* Adjusted left position */
            transform: translateY(-50%);
            color: #ccc;
            transition: all 0.3s;
        }

        input[type="text"]:focus + i,
        input[type="password"]:focus + i {
            color: #ffc107;
        }

        input[type="submit"] {
            width: 200px;
            padding: 15px;
            background-color: rgb(255, 230, 8);
            color: #fff;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.3s;
        }

        input[type="submit"]:hover {
            background-color: #ffc107;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Signin</h1>
        <form method="POST" action="/Signin">
            <div style="position: relative;">
                <input type="text" name="email" required placeholder="Email"><i class="fas fa-envelope" style="position: absolute; top: 35%; transform: translateY(-50%); left: 10px;"></i>
            </div>
            <div style="position: relative;">
                <input type="password" name="password" required placeholder="Password"><i class="fas fa-lock" style="position: absolute; top: 35%; transform: translateY(-50%); left: 10px;"></i>
            </div>
            <input type="submit" value="Signin">
        </form>
    </div>
</body>
</html>
